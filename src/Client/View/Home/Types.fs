module View.Home.Types

// A less-than-pretty basic counter serves as the gadget on the home page.
// So, our model consists of one Int
type Model
    = unit

// We can increment or decrement our int
type Msg = unit